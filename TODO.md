# TODO

## Backlog
- [ ] Ratings — requires Google Places API key; not in the list data source
- [ ] Type/category filter (e.g. Restaurant, Hotel) — requires Google Places API key
- [ ] Google Sheets export — one-click push via Sheets API (needs OAuth)
- [ ] Badge auto-extract — show place count in toolbar badge on list pages
- [ ] Multi-list merge — open multiple lists and combine into one export
- [ ] iOS app — accept a shared Maps list URL via Share Sheet, return CSV
